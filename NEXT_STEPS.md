# MVP 이후 작업 (NEXT_STEPS)

이 문서는 이번 MVP 범위에 의도적으로 포함하지 않은 항목들을 정리합니다. 지시서
4절("과도한 확장 개발을 피하고 현재 요구사항을 먼저 완성한다")에 따라 아래
항목들은 이번 범위에서 제외했습니다.

## 아키텍처/인프라

- **Caddy Gateway 전환 검토** — XAMPP Apache를 유지한 채로 두고, 향후 별도
  프로젝트로 Caddy 기반 게이트웨이 도입 여부를 검토. 이번 MVP는 Apache를
  대체하지 않습니다.
- **Windows 서비스 계정 최소 권한 전환** — 현재는 관리자 권한 실행을 전제로
  문서화(DEPLOY_WINDOWS_SERVER.md 9절)했습니다. 전용 서비스 계정 + `icacls` 기반
  최소 권한 부여로 전환하는 작업.
- **세션 저장소 이중화** — 현재 `express-session`은 MemoryStore(단일 프로세스,
  재시작 시 세션 소실)를 사용합니다. 관리자가 1~3명뿐인 이번 규모에서는
  허용 가능한 트레이드오프로 판단했습니다. 다중 인스턴스/무중단 재시작이
  필요해지면 파일 또는 SQLite 기반 세션 저장소로 교체해야 합니다.
- **백업 자동 정리(retention)** — 현재 백업은 무기한 누적됩니다. 디스크 용량
  관리를 위한 보관 기간/개수 정책 및 자동 삭제 기능.

## IoT / 장비 관제

- **MQTT 브로커 연동** — 현재 장비 관리는 기본 정보(ID/토큰/연결 프로그램)만
  다루며 실제 장비 통신 프로토콜은 구현하지 않았습니다.
- **WebSocket 장비 통신** — 장비 ↔ 관제 서버 간 실시간 데이터 채널.
- **장비 실시간 상태** — 현재 `lastConnectedAt` 필드는 존재하지만 이를 갱신하는
  실제 통신 경로가 없습니다 (수동/향후 장비 SDK 연동 필요).
- **공장별 그룹** — 장비/프로그램을 공장·현장 단위로 묶어 관리하는 기능.

## 인증/권한

- **사용자별 세분화된 권한** — 현재는 관리자 역할이 사실상 단일 등급입니다
  (`role: "ADMIN"` 필드는 있으나 권한 분기 로직은 없음). 조회 전용 계정 등
  세분화된 역할이 필요하면 이 필드를 기준으로 미들웨어를 확장합니다.
- **비밀번호 재설정 플로우** — 현재는 계정을 삭제 후 재생성하거나 시드
  스크립트를 다시 실행하는 방식만 있습니다.

## 운영 편의

- **프로그램 자동 실행 관리** — 대상 프로그램 자체를 이 시스템이 기동/재시작하는
  기능은 없습니다 (Reverse Proxy 설정만 관리).
- **서비스별 로그 스트리밍** — 각 프로그램의 Apache 액세스/에러 로그를 관리
  화면에서 실시간으로 보는 기능.
- **인증서 자동 갱신 연동** (예: win-acme, Certbot) 및 만료 임박 경고.
- **DNS API 연동** — 도메인 등록 시 실제 DNS 레코드까지 자동 생성.
- **프로그램별 설정 변경 이력(diff) 뷰어** — 현재 `ApacheConfigRevision`에
  매 적용 시점의 전체 설정 내용이 저장되고 있으나, 이를 이전 버전과 비교(diff)해
  보여주는 화면은 아직 없습니다. 데이터는 이미 쌓이고 있으므로 화면만 추가하면
  됩니다.

## 알려진 단순화 (버그 아님, 의도적 범위 제한)

- 도메인 변경 시 이전 도메인의 Apache 설정을 안전하게 제거하는 로직은 있지만,
  두 도메인을 동시에 유지하며 점진적으로 전환하는 기능(예: 블루/그린)은 없습니다.
- 헬스체크는 관리자가 수동으로 "연결 테스트"를 누르거나 적용 직후에만
  수행됩니다. 주기적인 백그라운드 자동 헬스체크(스케줄러)는 없습니다 — 다음
  우선순위 후보입니다.
