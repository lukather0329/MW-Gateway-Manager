# 문제 해결 가이드

## Apache 문법 오류 (Syntax error)

**증상**: 설정 미리보기까지는 정상이지만 "적용" 시 "Apache 문법 검사 실패로
이전 설정으로 복구했습니다" 메시지가 표시됨.

- 화면에 표시되는 `httpd.exe -t`의 원문 출력(오류 줄 번호 포함)을 확인하세요.
- 이 시스템이 생성한 파일 자체의 문법 오류라면 버그일 가능성이 높습니다 —
  **Apache 상태** 메뉴에서 "문법 검사 실행"으로 현재 활성 설정 전체를 다시 검사해
  다른 사이트의 기존 오류와 섞여있지 않은지 확인하세요.
- 적용은 실패했어도 **자동으로 이전 설정으로 복구되므로 기존 서비스는 영향받지
  않습니다.** 복구 자체가 실패한 경우(드묾)에는 APACHE_SETUP.md의 "수동 복구
  방법"을 따르세요.

## 포트 연결 실패 (연결 테스트에서 "연결 불가")

**증상**: 프로그램 목록/상세의 연결 상태가 "연결 불가"(UNREACHABLE).

- 대상 프로그램(예: 카메라 관제 서버)이 실제로 실행 중인지 먼저 확인하세요.
- 대상 IP/포트가 올바른지 확인하세요 (오타, 사설 IP 대역 오입력 등).
- 방화벽이 Node 서버 → 대상 IP:포트 간 통신을 막고 있지 않은지 확인하세요.
- **이 상태는 Apache 설정과 무관합니다.** Apache 설정은 이미 정상 적용되어
  있을 수 있으며(스펙 6.8 정책), 대상 프로그램이 켜지면 다시 "연결 테스트"를
  눌러 정상으로 바뀌는지 확인하면 됩니다.

## 404 응답 ("Cannot GET /" 등)

**증상**: 상태 확인 경로를 지정하지 않은 프로그램이 "HTTP 서버 응답함"으로
표시되고 오류로 취급되지 않음.

이는 버그가 아니라 의도된 동작입니다. 많은 백엔드 프레임워크는 `/`에 대해
404를 반환하더라도 서버 자체는 정상 동작 중입니다. 정확한 상태를 보려면
프로그램 등록/수정 화면에서 **상태 확인 경로**(예: `/api/health`)를 지정하세요.
경로를 지정하면 해당 경로가 2xx를 반환하는지로 "정상/상태 확인 실패"를
구분합니다.

## 502 Bad Gateway

**증상**: 실제 도메인으로 접속 시 Apache가 502를 반환.

- 대상 프로그램이 다운되었거나, 등록된 IP/포트가 실제 서비스 포트와 다를
  가능성이 높습니다. 관리 화면에서 해당 프로그램의 "연결 테스트"를 먼저
  실행하세요.
- `targetProtocol`(http/https)이 실제 백엔드와 일치하는지 확인하세요. 백엔드가
  자체 서명 인증서를 쓰는 https 서버라면 `https`로 등록해야 합니다.

## SSL 인증서 오류

**증상**: 적용은 성공했지만 브라우저에서 인증서 경고가 뜨거나, Apache 문법
검사에서 인증서 경로 오류가 발생.

- **시스템 설정** 메뉴에서 `SSL 인증서 경로` / `SSL 개인키 경로`가 실제 파일과
  일치하는지 확인하세요.
- **Apache 상태 > 최초 설정 마법사 검사**가 두 파일의 존재 여부를 보여줍니다
  (✘ 표시면 경로가 틀렸거나 파일이 없는 것입니다).
- 인증서가 만료되었는지 별도로 확인하세요 (이 시스템은 인증서 유효기간을
  검사하지 않습니다 — NEXT_STEPS.md 참고).

## WebSocket 실패

**증상**: WebSocket 연결이 즉시 끊기거나 502/400 오류.

- "설정 미리보기" 또는 **Apache 상태** 화면에서 `mod_proxy_wstunnel` 모듈이
  활성 상태인지(WebSocket 모듈: 사용 가능) 확인하세요. 비활성이면 이 시스템이
  적용 자체를 차단합니다 — APACHE_SETUP.md의 모듈 활성화 절차를 따르세요.
- 대상 프로그램이 실제로 WebSocket 서버인지, 등록된 포트가 맞는지 확인하세요.

## 파일 권한 오류

**증상**: 적용 시 "설정 파일 쓰기 실패" 메시지.

- Node 프로세스를 실행하는 계정이 `mw-sites`, `mw-backups` 폴더에 쓰기 권한이
  있는지 확인하세요 (`icacls` 명령으로 확인/부여 — DEPLOY_WINDOWS_SERVER.md 9절
  참고).
- 이 경우 아직 아무 파일도 변경되지 않았으므로(쓰기 자체가 실패) 별도의 롤백
  절차가 필요하지 않습니다.

## 설정 재적용(reload) 실패

**증상**: "Apache 설정 재적용(reload) 실패로 이전 설정으로 복구했습니다."

- **가장 흔한 원인 (실제 확인됨)**: Apache가 Windows 서비스로 등록되지 않은
  채 그냥 프로세스로 실행 중인 경우입니다. 이 경우 오류 메시지에
  `AH00436: No installed service named "Apache2.4"`가 포함됩니다. 해결:
  관리자 권한으로 `httpd.exe -k install` 후 `net start Apache2.4` (자세한
  내용은 APACHE_SETUP.md 6절).
- 문법은 통과했지만 reload 자체가 실패한 다른 경우도 있습니다. Apache 프로세스가
  응답 없는 상태이거나 다른 프로세스가 포트를 점유하고 있을 수 있습니다.
- 참고: 이 시스템은 내부적으로 `httpd.exe -k restart`를 사용합니다 (Windows는
  무중단 graceful 재적재를 지원하지 않음). 설정이 반영되는 짧은 순간 진행 중이던
  연결이 끊길 수 있는 것은 정상입니다.
- Windows 작업 관리자 또는 `tasklist /FI "IMAGENAME eq httpd.exe"`로 Apache
  프로세스 상태를 확인하세요.
- 자동으로 이전 설정을 복구하고 다시 reload를 시도합니다. 그래도 실패하면
  Apache 서비스를 수동으로 재시작(`httpd.exe -k restart`)해야 할 수 있습니다 —
  이는 순간적인 서비스 중단을 유발하므로 신중히 진행하세요.

## 설정 롤백 / 백업 복구

- 자동 롤백 로그는 **변경 이력** 메뉴에서 `APACHE_APPLY_FAILURE`,
  `PROGRAM_*` 액션으로 확인할 수 있습니다.
- 특정 시점으로 수동 복구하려면 **설정 백업** 메뉴에서 원하는 백업의
  "이 시점으로 복구" 버튼을 사용하세요. 복구 후에도 문법 검사를 통과해야만
  실제로 reload가 실행됩니다.
- 자동 복구 자체가 실패하는 극단적 상황은 APACHE_SETUP.md 7절의 수동 복구
  절차를 따르세요.
